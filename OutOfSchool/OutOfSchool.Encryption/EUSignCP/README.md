# End User Signing Library

The code is provided as part of the library packages and is used as is.

While the library is (hopefully) transitioning to being publicly available Nuget package this code is used to
compile the application in Development mode.

In Release mode the namespace is going to be replaced by Nuget package.