# apiVersion: v1
# kind: Pod
# metadata:
#   name: "{{ include "outofschool.fullname" . }}-test-connection"
#   annotations:
#     "helm.sh/hook": test
# spec:
#   containers:
#     - name: wget
#       image: busybox
#       command: ['wget']
#       args: ['{{ include "outofschool.fullname" . }}:{{ .Values.service.port }}']
#   restartPolicy: Never
