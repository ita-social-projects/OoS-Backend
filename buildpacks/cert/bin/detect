#!/usr/bin/env bash
set -eo pipefail

if [[ -z "${BP_CA_JSON_URL}" || -z "${BP_CA_P7B_URL}" ]]; then
    echo 'Both `BP_CA_JSON_URL` and `BP_CA_P7B_URL` variable should be set' && exit 100
else
    exit 0
fi